# /etc/proxmox-mcp/env
# Loaded by systemd EnvironmentFile=. chmod 600, owned by root:mcp.

PROXMOX_MCP_CONFIG=/etc/proxmox-mcp/config.json
PROXMOX_ALLOWED_POOL=ai-redteam
MCP_BEARER_TOKEN_FILE=/etc/proxmox-mcp/bearer
MCP_BIND=0.0.0.0:8080
LOG_LEVEL=info
